@model IEnumerable<prjProyectoRestaurante.Models.InventarioProdu>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantidadMinima)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadMinima)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDIventaarioProdu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDIventaarioProdu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDIventaarioProdu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
