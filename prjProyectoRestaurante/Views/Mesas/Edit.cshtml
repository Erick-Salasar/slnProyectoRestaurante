@model prjProyectoRestaurante.Models.Mesa

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Mesa</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IDMesa" />
            <div class="form-group">
                <label asp-for="NumeroMesa" class="control-label"></label>
                <input asp-for="NumeroMesa" class="form-control" />
                <span asp-validation-for="NumeroMesa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IDEstado" class="control-label"></label>
                <select asp-for="IDEstado" class="form-control" asp-items="ViewBag.IDEstado"></select>
                <span asp-validation-for="IDEstado" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IDReservaMesa" class="control-label"></label>
                <select asp-for="IDReservaMesa" class="form-control" asp-items="ViewBag.IDReservaMesa"></select>
                <span asp-validation-for="IDReservaMesa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
