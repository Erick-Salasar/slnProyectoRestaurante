@model IEnumerable<prjProyectoRestaurante.Models.Mesa>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroMesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoMesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReservaMesa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroMesa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoMesa.IDEstado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReservaMesa.IDReservaMesa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDMesa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDMesa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDMesa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
