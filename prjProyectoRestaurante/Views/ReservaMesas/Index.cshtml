@model IEnumerable<prjProyectoRestaurante.Models.ReservaMesa>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoraReserva)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraReserva)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDReservaMesa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDReservaMesa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDReservaMesa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
