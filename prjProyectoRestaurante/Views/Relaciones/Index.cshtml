@model IEnumerable<prjProyectoRestaurante.Models.Relacion>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.IDPersona)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDRelacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDRelacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDRelacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
